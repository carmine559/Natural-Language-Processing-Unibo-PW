# Prometeia pipeline — cluster requirements (CUDA 11.8).
#
# torch is NOT listed here: install it FIRST against the cu118 index, per the
# DISI cluster instructions, e.g.
#   pip3 install torch --no-cache-dir --index-url https://download.pytorch.org/whl/cu118
# (setup_env.sh does this for you.)
#
# Always pass --no-cache-dir on the cluster (the 400 MB home quota fills fast).

transformers>=4.51   # Qwen3 support requires >=4.51
accelerate>=0.30
bitsandbytes>=0.43
pydantic>=2.0
rank-bm25>=0.2
pandas>=2.0
huggingface_hub>=0.34  # provides the `hf` CLI (huggingface-cli is removed in 1.x)

# Optional — only needed with --use-groq:
# groq>=0.11

# Optional — better Italian tokenization for BM25 (falls back to regex if absent):
# spacy>=3.7
